---
import generateUrlPath from "@utils/generateUrlPath";

interface Props {
  anime: {
    list: AnimeCard1[];
    baseUrlPath: string;
  };
}

const {
  anime: { list, baseUrlPath },
} = Astro.props;
---

<div class={`py-3 gap-4 grid xs:grid-cols-2 sm:grid-cols-3 md:grid-cols-4`}>
  {
    list.map((anime) => (
      <a
        href={generateUrlPath(
          baseUrlPath,
          anime.animeId || anime.batchId || ""
        )}
        class="group relative"
      >
        <div class="relative overflow-hidden rounded-lg">
          <img
            class="anime1-item-img"
            data-src={anime.poster}
            alt={anime.title}
            src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 400 600'%3E%3Crect fill='%23374151' width='400' height='600'/%3E%3C/svg%3E"
          />
          <span class="anime1-item-eps">Ep {anime.episodes}</span>
        </div>
        <div class="p-2">
          <h3 class="anime1-item-title">{anime.title}</h3>
        </div>
      </a>
    ))
  }
</div>
